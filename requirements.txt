# ============================================================
# 面向动态场景的三维障碍物检测与分割系统研究 — 依赖清单
# Python >= 3.10，兼容 Windows / Linux
# ============================================================

# ── 核心依赖（必须安装）────────────────────────────────────

# 数值计算
numpy>=1.23.0

# 3D 点云读取与可视化
open3d>=0.17.0

# GUI 框架
PyQt5>=5.15.0
PyQt5-sip>=12.11

# 配置文件解析
PyYAML>=6.0

# nuScenes mini 数据集索引（会附带 matplotlib / opencv-python 等）
nuscenes-devkit>=1.1.9

# ── 实时感知依赖（实时模式需要）────────────────────────────

# DBSCAN 聚类加速（优先路径，不安装则自动降级到 scipy 手动实现）
scikit-learn>=1.3.0

# RANSAC / DBSCAN 降级实现（scikit-learn 不可用时的备选）
scipy>=1.11.0

# Intel RealSense 深度相机驱动（仅使用 RealSense 实时模式时需要）
# 未安装时 Mock Camera 和所有离线功能仍可正常使用
# pyrealsense2>=2.53.1

# ── 可选：OpenPCDet 真实推理（运行在 WSL2 Ubuntu 中，非 Windows 端依赖）──
# OpenPCDet 在 WSL2 中独立安装，Windows 端通过 subprocess 调用，无需在此安装
# 参考 WSL 安装步骤：https://github.com/open-mmlab/OpenPCDet
# torch>=2.0.0        # 安装在 WSL2 conda 环境中
# torchvision>=0.15.0
